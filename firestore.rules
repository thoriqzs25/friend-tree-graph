rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Single-owner graph — path contains the owner ID as a secret segment.
    // Access control is handled by the PIN gate at the app layer.
    match /owners/{ownerId}/graphs/{graphId} {
      allow read, write: if true;
    }
  }
}
